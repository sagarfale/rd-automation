---
  - name: Shell Examples
    hosts: "{{ v_host | default('') }}"
    tasks:

    - name: Check Date with Shell command
      shell:
         echo "$RD_OPTION_HOSTNAME"
         echo "$$RD_OPTION_COMMAND"
