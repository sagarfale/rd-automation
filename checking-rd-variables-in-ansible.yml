---
  - name: Shell Examples
    hosts: "{{ v_host | default('') }}"
    tasks:

    - name: Check Date with Shell command
      shell:
         echo "@option.command@" > /tmp/t2.log
         echo "@option.hostname@"  >> /tmp/t2.log
